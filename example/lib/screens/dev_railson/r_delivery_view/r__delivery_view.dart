import 'package:flutter/material.dart';

class RDelivery extends StatelessWidget {
  const RDelivery({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        alignment: Alignment.center,
        child: Text("Railson's Delivery"),
      ),
    );
  }
}
